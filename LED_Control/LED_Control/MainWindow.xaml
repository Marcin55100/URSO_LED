<Window x:Class="LED_Control.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LED_Control"
        mc:Ignorable="d"
        Title="Menu" Height="350" Width="525">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>


        <DockPanel Grid.Row="1" Grid.Column="1">

            <StackPanel DockPanel.Dock="Top">
                <!-- Width="140" Height="100"-->
            <Button x:Name="LedButton" Content="Sterowanie LED" Click="LedButton_Click" />
            <Button x:Name="GroupButton" Content="Ustawienia portów" Click="GroupButton_Click" />
            <Button x:Name="ConfigButton" Content="Konfiguracja połączenia" Click="ConfigButton_Click" />
            <Label x:Name="Infolabel" Content="cos" />
            </StackPanel>
        </DockPanel>

    </Grid>
</Window>
